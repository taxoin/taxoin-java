quarkus.http.port=47780
quarkus.http.cors=true
quarkus.http.cors.origins=*
quarkus.application.name=taxoin
taxoin.dir=.taxoin

# Bouncy Castle must be loaded by parent classloader to avoid cast failures
quarkus.class-loading.parent-first-artifacts=org.bouncycastle:bcprov-jdk18on

# Test profile — use temp dir
%test.taxoin.dir=target/taxoin-test
